name: art-gallery
replicas: 1
containers:
  - path: .
    name: app
    kube:
      ports:
        - container_port: 8080
      env:
        - name: PORT
          value: "8080"
http:
  - port: 8080
    subdomain: art-gallery
